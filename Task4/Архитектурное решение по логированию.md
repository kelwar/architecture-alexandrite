Необходимо, в первую очередь, собирать логи уровня INFO в местах прохождения значимых запросов, связанных непосредственно с заказом:
1. Любое изменение статуса заказа, связанное с действиями пользователя.
2. Подтверждение готовности заказа к обработке со стороны CRM API.
3. Получение запроса системой MES API как от пользователей API, так и из очереди RabbitMQ.

Состав записи журнала:
1. Время события (таймстамп).
2. Номер заказа.
3. Название системы (например MES-API, Shop-API и т.д.)
4. Тип пользователя (Customer, Seller, Operator).
5. Идентификатор пользователя в системе.
6. Сообщение.

Логи уровня ERROR/WARN - для нештатных ситуаций.
Состав:
1. Время события (таймстамп).
2. Название системы (например MES-API, Shop-API и т.д.)
3. Тип пользователя (Customer, Seller, Operator).
4. Идентификатор пользователя в системе.
5. Сообщение.
6. Название потока (thread).
7. Stack Trace (при наличии).
8. HTTP Servlet Path - endpoint, при вызове которого возникла ошибка/предупреждение.
9. HTTP Status Value - код ошибки HTTP.
10. HTTP Status Reason - текст ошибки HTTP.

Логи уровня ERROR отправлять на эл.почту тех.поддержки.
В случае выявления странного поведения системы, допустимо использовать логи уровня DEBUG в качестве временной меры для выявления и устранения причин возникновения странного поведения системы.

### Мотивация

Внедрение логирования позволит существенно снизить время на обработку пользовательских заявок в техподдержку. 
Большинство запросов сотрудник техподдержки сможет решить самостоятельно, не привлекая разработчиков.
Разработчикам потребуется значительно меньше времени на поиск и выявление причин неисправности, что в том числе скажется положительно на скорости внедрения нового функционала, а также улучшит пользовательский опыт взаимодействия с техподдержкой.

В первую очередь необходимо настроить логирование и трейсинг для систем CRM-API, MES-API, т.к. именно на этом участке на данный момент происходит задержка прохождения запросов.
Также, рекомендуется настроить логирование системы Shop-API в части прохождения запросов, связанных с изменением статуса заказа, для обеспечения целостной картины жизненного цикла заказа внутри системы приложений.

### Предлагаемое решение

На рынке существует несколько решений для работы с логами. Например, Splunk, ElasticSearch, OpenSearch, Loki.
Loki заточен под большие объемы данных, в то время как рассматриваемая задача предполагает логирование только в части обработки заказов, а также системных ошибок.
Учитывая цели логирования (фактически ведение журнала обработки заказов), предлагается не использовать Splunk, т.к. это слишком мощное платное решение для рассматриваемой задачи.
При выборе между ElasticSearch и OpenSearch, предлагается отдать предпочтение ElasticSearch, т.к. это зрелый продукт с хорошо налаженным сообществом и обширной документацией.
OpenSearch распространяется с лицензией Apache 2.0, что является преимуществом в плане финансовых затрат, однако потенциальные затраты на внедрение играют более важную роль в принятии решения о выборе.

Диаграмма:
./jewerly_c4_model.drawio

Реализация (ELK-стек):
1. Filebeat будет собирать данные из сервисов Shop-API, CRM-API и MES-API и передавать в Logstash - конвейер данных.
2. Logstash передаёт данные на хранение в Elasticsearch.
3. Elasticsearch обменивается данными с Kibana - инструментом визуализации.

Доступ к логам должны иметь только сотрудники с ролью "Поддержка". В логах не будут использованы чувствительные данные - пользователи и их заказы будут определяться по ИД пользователя и номеру заказа соответственно.

Будет использован отдельный индекс под систему. Срок хранения логов: 3 месяца, т.к. по жалобам пользователей некоторые заказы обрабатываются несколько месяцев. 
Более долгий срок хранения требует дополнительных существенных вложений в дисковое пространство и нецелесообразен.
Размер лог-файла - 128МБ.

Также, необходимо настроить оповещение о важных событиях на электронную почту. Например, получение сообщения уровня ERROR.
Внедрение системы поиска аномалий с использованием ИИ, на данном этапе нецелесообразно. 

Однако, стоит внедрить систему поиска аномалий на основе пороговых значений, а именно:
1. Искать такие логи об установке статуса SUBMITTED, после которых не установлен другой статус в течение 1 часа.
2. Искать такие логи об установке статуса MANUFACTURING_APPROVED, после которых не установлен другой статус в течение 3 часов.
О результатах сработки оповещать саппорт через эл.почту.

Аномально большое количество новых заказов анализировать на данном этапе смысла не имеет, т.к. пока заказ не получил статус MANUFACTURING_APPROVED от менеджера в CRM-системе, он не отдаётся в работу.
Предполагается, что менеджеры в состоянии самостоятельно отследить подобные аномалии.